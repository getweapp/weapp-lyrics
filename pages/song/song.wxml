<view class="panel panel-success">
  <view class="panel-heading">{{songTitle}}</view>
  <view class="panel-body">
    <view class="lrc" wx:for="{{songTexts}}" wx:key="item.id">{{item}}</view>
  </view>
</view>